"""\nJev AI Configuration Module\nLoads environment variables and validates settings\n"""\n\nfrom pydantic_settings import BaseSettings, SettingsConfigDict\nfrom pydantic import Field, field_validator\nfrom typing import Optional\nimport os\n\n\nclass Settings(BaseSettings):\n    """Application settings loaded from environment variables"""\n    \n    model_config = SettingsConfigDict(\n        env_file=".env",\n        env_file_encoding="utf-8",\n        case_sensitive=False,\n        extra="ignore"\n    )\n    \n    # ============================================\n    # CLAUDE CODE SESSION\n    # ============================================\n    claude_code_oauth_token: str = Field(\n        ...,\n        description="Claude Code OAuth session token (from /login)"\n    )\n    \n    # ============================================\n    # TELEGRAM\n    # ============================================\n    telegram_bot_token: str = Field(\n        ...,\n        description="Telegram bot token from @BotFather"\n    )\n    \n    telegram_chat_id: str = Field(\n        ...,\n        description="Your Telegram chat ID for notifications"\n    )\n    \n    # ============================================\n    # MCP SERVERS\n    # ============================================\n    mcp_telegram_url: str = Field(\n        default="http://localhost:8765",\n        description="Telegram MCP server URL"\n    )\n    \n    mcp_polymarket_url: str = Field(\n        default="http://localhost:8766",\n        description="Polymarket MCP server URL"\n    )\n    \n    # ============================================\n    # DATABASE\n    # ============================================\n    database_url: str = Field(\n        default="postgresql://postgres:postgres@localhost:5432/polymarket",\n        description="PostgreSQL connection string"\n    )\n    \n    # ============================================\n    # COPY TRADING RULES\n    # ============================================\n    min_trades_90d: int = Field(\n        default=20,\n        ge=1,\n        description="Minimum trades in last 90 days"\n    )\n    \n    min_lifetime_pnl: float = Field(\n        default=10000.0,\n        ge=0,\n        description="Minimum lifetime realized PnL in USD"\n    )\n    \n    min_win_rate: float = Field(\n        default=0.20,\n        ge=0.0,\n        le=1.0,\n        description="Minimum win rate (0.0-1.0)"\n    )\n    \n    max_positions: int = Field(\n        default=10,\n        ge=1,\n        le=50,\n        description="Maximum simultaneous copy positions"\n    )\n    \n    position_size_usdc: float = Field(\n        default=50.0,\n        ge=1.0,\n        description="Position size per copy trade in USDC"\n    )\n    \n    copy_sells: bool = Field(\n        default=True,\n        description="Whether to copy sell/exits"\n    )\n    \n    poll_interval_secs: int = Field(\n        default=60,\n        ge=10,\n        description="Poll interval for new whale trades (seconds)"\n    )\n    \n    # ============================================\n    # POLYMARKET API\n    # ============================================\n    gamma_api_url: str = Field(\n        default="https://gamma-api.polymarket.com",\n        description="Polymarket Gamma API base URL"\n    )\n    \n    gamma_requires_auth: bool = Field(\n        default=False,\n        description="Whether Gamma API requires authentication"\n    )\n    \n    # ============================================\n    # LOGGING\n    # ============================================\n    log_level: str = Field(\n        default="INFO",\n        description="Logging level"\n    )\n    \n    log_file: Optional[str] = Field(\n        default=None,\n        description="Log file path"\n    )\n    \n    @field_validator('log_level')\n    @classmethod\n    def validate_log_level(cls, v: str) -> str:\n        valid_levels = ['DEBUG', 'INFO', 'WARNING', 'ERROR', 'CRITICAL']\n        if v.upper() not in valid_levels:\n            raise ValueError(f"log_level must be one of {valid_levels}")\n        return v.upper()\n    \n    # ============================================\n    # COMPUTED PROPERTIES\n    # ============================================\n    @property\n    def is_production(self) -> bool:\n        """Check if running in production mode"""\n        return os.getenv('ENVIRONMENT', 'development') == 'production'\n    \n    @property\n    def debug_mode(self) -> bool:\n        """Check if debug mode is enabled"""\n        return self.log_level == 'DEBUG'\n\n\n# Global settings instance\nsettings = Settings()\n\n\ndef get_settings() -> Settings:\n    """Dependency injection for settings"""\n    return settings\n